{
  "name": "@hilma/secrets-handler",
  "version": "1.0.1",
  "description": "configuration and handling of secrets",
  "main": "main.ts",
  "types": "secrets-handler.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tsc && node dist/main.js",
    "prepare": "npm run build",
    "prebuild": "rm -rf /dist",
    "build": "npx tsc && cp secrets-handler.d.ts dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hilma-tech/secrets-handler.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/hilma-tech/secrets-handler/issues"
  },
  "homepage": "https://github.com/hilma-tech/secrets-handler#readme",
  "dependencies": {
    "aws-sdk": "^2.1187.0"
  },
  "devDependencies": {
    "@types/node": "^18.14.6",
    "typescript": "^4.7.4"
  }
}
